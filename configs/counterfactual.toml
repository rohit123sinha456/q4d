[project]
seed = 7
output_dir = "artifacts/datasets/pushcube_counterfactual_v1"

[model]
n_scene_points = 256
n_query_points = 32
horizon = 8
width = 128

[simulation]
env_id = "PushCube-v1"
num_envs = 1
obs_mode = "rgb+depth+segmentation"
control_mode = "pd_ee_delta_pose"
steps = 8
sim_backend = "physx_cpu"
render_backend = "sapien_cpu"

[labels]
num_points = 256
horizon = 8
max_depth_m = 1.5
object_quota = 24
robot_quota = 64
goal_quota = 16
approach_max_steps = 16
contact_distance_m = 0.03
